 #!/usr/local/bin/ruby -w
def fibo ()
  A[]
  i
  f=0
    for i in 0...A.length
       A[index]=i
        if i>=2
          A[i]=A[i-1]+A[i-2]
            if i=f
              puts"this Fibonumber=#{A[i]}"
            end
            end
        end
        i++  

